# Create Container by command
1. Change mapping directory "D:/Docker/db_postgresql"
2. Change root password in docker-compose.yml "POSTGRES_PASSWORD=1234"

```
docker network create db_network
```

## Postgresql latest
```
docker-compose up
```